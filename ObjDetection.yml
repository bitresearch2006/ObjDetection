provider:
  name: openfaas
  gateway: http://127.0.0.1:8080

functions:
  objdetection:
    lang: python3-http-debian
    handler: ./main
    image: localhost:5000/objdetection:latest
    min_replicas: 1
    build_args:
      ADDITIONAL_PACKAGE: ""           
    environment:
      DIAGNOSTICS: "true"
      LOG_DIR: "/tmp/obj_detection_logs"  
